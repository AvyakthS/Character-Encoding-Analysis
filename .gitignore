# 1. Ignore User Datasets (but keep the folder structure)
user_bench_files_freesize/*
!user_bench_files_freesize/.gitkeep

# 2. Ignore Standardized Generated Datasets
user_bench_files_standardized/*
!user_bench_files_standardized/.gitkeep

# 3. Ignore Benchmark Results & Logs inside version folders
# (We assume you want to keep the scripts, but not the CSVs/Reports generated by runs)
versions/**/*.csv
versions/**/*.txt
versions/**/test_output.*

# 4. Ignore Python Cache
__pycache__/
*.pyc
